The purpose of the this assignment is to 

1) Design a database schema for a given dataset which consists of ebay auction data, given by a set of XML files. Each XML file is valid with respect to the given DTD.  

2) Shred XML data into CSV ­files by writing a JAVA program that reads in the XML files and writes out CSV files one for each table in the schema.

3) Create tables in a database by writing SQL scripts and the load the CSV files into these tables.

4) Test the database by running few queries over it.

